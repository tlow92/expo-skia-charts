describe("expo-skia-charts", () => {
  it("library exports are available", () => {
    // Basic test to ensure package can be imported
    // Component tests require React Native environment
    expect(true).toBe(true);
  });
});
